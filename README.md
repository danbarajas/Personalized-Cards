# Personalized Cards
ACM WebDev SiG Project 1.


## Setup
1. Navigate to the PERSONALIZED-CARDS folder.
2. Run `npm i` to install necessary dependencies.
3. Run `npm run dev` to run the website locally.

## Contribute
Choose a folder. Make sure no one else is using that one. You can change the JSX and CSS file inside that folder.